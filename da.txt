from stem import Signal
from stem.control import Controller
import time

print("IP rotation started - every 30 seconds")
while True:
    with Controller.from_port(port=9051) as c:
        c.authenticate()
        c.signal(Signal.NEWNYM)
        print(f"New IP at {time.strftime('%H:%M:%S')}")
    time.sleep(30)
